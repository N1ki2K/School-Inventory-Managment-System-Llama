version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  # Optional: Add backend service when available
  # backend:
  #   image: your-backend-image
  #   ports:
  #     - "4000:4000"
  #   environment:
  #     - NODE_ENV=production
  #   restart: unless-stopped
